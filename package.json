{
  "name": "material-ui-multibrand-design-system",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-brand Material UI design system driven by Figma variables — 6 brands, one token pipeline",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:tokens": "npm run build:tokens -w @multibrand/design-system",
    "build:lib": "npm run build -w @multibrand/design-system",
    "build:demo": "npm run build -w demo",
    "build": "npm run build:tokens && npm run build:lib && npm run build:demo",
    "dev": "npm run dev -w demo",
    "typecheck": "npm run typecheck -w @multibrand/design-system && npm run typecheck -w demo"
  },
  "engines": {
    "node": ">=20"
  }
}
